<%- include('../../partials/header') %>
<%- include('../../partials/navbar') %>
<div class="container">
  <hr>
  <div class="card">
    <div class="card-header">
      <h5>Editar a categoria "<%= category.title %>" </h5>
    </div>
    <div class="card-body">
      <form method="POST" action="/admin/categories/update">
        <input class="form-control" type="text" name="title" placeholder="Título da categoria" value="<%= category.title %>">
        <br>
        <input type="hidden" name="id" value="<%= category.id %>">
        <button class="btn">Atualizar</button>
      </form>
    </div>
  </div>
</div>
<%- include('../../partials/footer') %>
<script>
  const titleElement = document.getElementById('title-html');
  const dynamicTitle = "Criar nova categoria - DevZone";
  titleElement.textContent = dynamicTitle;
</script>
